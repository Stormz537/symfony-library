{% extends 'base.html.twig' %}

{% block body %}
    <div class="main mt-5">
        <div class="container text-center">
            <div class="introduction">
                <h2>Bienvenue {{ app.user.prenom }} dans le gestionnaire de bibliothèque</h2>
            </div>
        </div>
        <a href={{ path('livre_index') }}>Livres</a>
        <a href={{ path('category_index') }}>Catégories</a>
        {% if app.user %}
            <div>
                <a href="{{ path('app_logout') }}">Logout</a>
            </div>
        {% endif %}
        </div>
{% endblock %}