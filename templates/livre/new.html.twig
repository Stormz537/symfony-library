{% extends 'base.html.twig' %}

{% block title %}Nouveau Livre{% endblock %}

{% block body %}
    <div class="container">
        <h1>Ajouter un livre</h1>

        {{ include('livre/_form.html.twig') }}

        <a href="{{ path('livre_index') }}"><button class="btn btn-sm btn-secondary">Retour à la liste</button></a>
    </div>
{% endblock %}
