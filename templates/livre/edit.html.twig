{% extends 'base.html.twig' %}

{% block title %}MàJ Livre{% endblock %}

{% block body %}
    <div class="container">
        <div class="mt-5">
            <h1>Mise à jour du livre</h1>
                {{ include('livre/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
                {{ include('livre/_delete_form.html.twig') }}
                <a href="{{ path('livre_index') }}"><button class="btn btn-sm btn-secondary">Retour à la liste</button></a>
        </div>
    </div>
{% endblock %}
